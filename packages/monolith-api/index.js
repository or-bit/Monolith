const controller = require('monolith-api-controller');
const model = require('monolith-api-model');
const view = require('monolith-api-view');

console.log(controller);
console.log(model);
console.log(view);

module.exports = {
  controller,
  model,
  view
};
