{
  "name": "monolith-routing-utils",
  "version": "1.0.0",
  "description": "Routing utils for monolith-framework",
  "main": "index.js",
  "scripts": {
    "coverage": "nyc --reporter=html --reporter=lcov --reporter=text mocha --recursive src",
    "lint": "eslint index.js src/**/*.js",
    "start": "node index.js",
    "test": "mocha --recursive src"
  },
  "keywords": [
    "monolith"
  ],
  "author": "Or-bit",
  "license": "MIT",
  "devDependencies": {
    "chai": "^4.0.2",
    "eslint": "^3.19.0",
    "eslint-config-airbnb-base": "^11.2.0",
    "eslint-plugin-chai-friendly": "^0.3.6",
    "eslint-plugin-mocha": "^4.10.1",
    "mocha": "^3.4.2",
    "nyc": "^11.0.2"
  }
}
