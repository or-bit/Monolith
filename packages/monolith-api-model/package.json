{
  "name": "monolith-api-model",
  "version": "0.0.3",
  "description": "",
  "main": "build/index.js",
  "scripts": {
    "compile": "build",
    "coverage": "nyc --reporter=html --reporter=text mocha --recursive src",
    "prepublish": "npm run compile",
    "test": "mocha --recursive src",
    "watch": "watch"
  },
  "author": "Or-Bit",
  "license": "MIT",
  "dependencies": {
    "axios": "^0.16.1",
    "express": "^4.15.2",
    "mongodb": "^2.2.26",
    "socket.io": "^2.0.1"
  },
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "builder": "^0.1.0",
    "chai": "^3.5.0",
    "chai-as-promised": "^6.0.0",
    "mocha": "^3.3.0",
    "mongo-in-memory": "0.0.5",
    "nock": "^9.0.13",
    "nyc": "^10.3.0",
    "socket.io-client": "^2.0.1",
    "tcp-ping": "^0.1.1"
  }
}
